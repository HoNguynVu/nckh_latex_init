\chapter{DỮ LIỆU VÀ QUY TRÌNH GÁN NHÃN}

\section{Nguồn dữ liệu}
\subsection{Dữ liệu văn bản}
Dữ liệu văn bản được thu thập từ các nền tảng mạng xã hội như TikTok và Facebook, ưu tiên các nội dung liên quan đến quảng cáo thực phẩm chức năng, thực phẩm bảo vệ sức khỏe và các nhóm sản phẩm sức khỏe phổ biến. Nguồn văn bản bao gồm caption, mô tả bài đăng, bình luận, phụ đề, transcript và nội dung OCR/ASR được trích xuất từ ảnh hoặc video.

Mỗi mẫu văn bản cần được lưu kèm thông tin nguồn, thời điểm thu thập nếu có, định danh bài đăng hoặc video và metadata cần thiết. Các trường này giúp truy vết dữ liệu, kiểm tra trùng lặp và hạn chế leakage khi chia tập.

\subsection{Dữ liệu hình ảnh}
Dữ liệu hình ảnh bao gồm ảnh trong bài đăng, frame được cắt từ video và bộ dữ liệu hình ảnh được quản lý trên Roboflow. Trong phiên bản thực nghiệm hiện tại, dataset hình ảnh sử dụng workspace Roboflow với tên \texttt{doctor-and-medical}, version 7 và export theo định dạng YOLO. Dữ liệu tập trung vào các đối tượng có thể gợi bối cảnh y tế hoặc dấu hiệu mạo danh/lợi dụng uy tín trong quảng cáo.

Các frame trích từ video cần được lọc để giảm trùng lặp, ví dụ chỉ giữ các frame có mức khác biệt đủ lớn so với frame liền trước. Cách làm này giúp giảm số lượng ảnh gần giống nhau và hạn chế nguy cơ mô hình học lệch theo các frame lặp.

\section{Quy trình thu thập và xử lý dữ liệu}
\subsection{Dữ liệu văn bản}
Quy trình dữ liệu văn bản gồm các bước chính:
\begin{enumerate}
    \item Xác định từ khóa, nhóm sản phẩm và danh sách URL cần thu thập.
    \item Thu thập bài đăng, caption, mô tả, bình luận, phụ đề hoặc transcript từ nền tảng nguồn.
    \item Chuẩn hóa Unicode, loại bỏ mẫu rỗng, kiểm tra trùng lặp và gán \texttt{post\_id}.
    \item Gán nhãn đa nhãn theo nội dung thực tế và lưu evidence nếu có.
    \item Chia dữ liệu thành train, validation và test theo quy tắc hạn chế leakage.
\end{enumerate}

\subsection{Dữ liệu hình ảnh}
Quy trình dữ liệu hình ảnh gồm các bước chính:
\begin{enumerate}
    \item Tải ảnh hoặc video từ các đường dẫn media hợp lệ bằng công cụ phù hợp.
    \item Cắt video thành frame và lọc các frame gần trùng lặp.
    \item Đưa ảnh/frame vào Roboflow để gán nhãn đối tượng bằng bounding box.
    \item Review thủ công các nhãn dễ nhầm lẫn như \texttt{Lab-coat} và \texttt{Shirt}.
    \item Export dataset version 7 theo YOLO format để huấn luyện bằng Ultralytics YOLO.
\end{enumerate}

\section{Định dạng dữ liệu}
\subsection{Dữ liệu văn bản}
Dữ liệu văn bản trong đề tài được lưu ở dạng bảng, trong đó mỗi dòng tương ứng với một đoạn văn bản sau khi tách từ bài đăng gốc. Các trường \texttt{post\_id}, \texttt{chunk\_id} và \texttt{split} được giữ lại để truy vết nguồn và chia tập theo bài đăng, tránh để các đoạn của cùng một bài xuất hiện đồng thời ở train và test.

\begin{table}[H]
\centering
\caption{Định dạng dữ liệu văn bản sử dụng trong đề tài}
\label{tab:text_data_format}
\begin{tabularx}{\textwidth}{|L{0.36\textwidth}|Y|}
\hline
\textbf{Nhóm trường} & \textbf{Ý nghĩa} \\
\hline
\texttt{source\_index}, \texttt{post\_id}, \texttt{chunk\_id} & Các trường định danh và truy vết. \texttt{source\_index} xác định nguồn hoặc dòng dữ liệu ban đầu; \texttt{post\_id} xác định bài đăng gốc; \texttt{chunk\_id} xác định đoạn văn bản sau khi tách. \\ \hline
\texttt{split} & Tập dữ liệu tương ứng, ví dụ train, validation hoặc test. Trường này được tạo sau bước chia tập và cần ưu tiên chia theo \texttt{post\_id}. \\ \hline
\texttt{text}, \texttt{url} & Nội dung văn bản đầu vào và đường dẫn nguồn. Trường \texttt{text} có thể là caption, mô tả, phụ đề, transcript, OCR hoặc ASR; \texttt{url} giúp kiểm tra lại ngữ cảnh khi cần. \\ \hline
\texttt{EFFICACY\_CLAIM}, \texttt{CURE\_CLAIM}, \texttt{AUTHORITY\_REFERENCE}, \texttt{TESTIMONIAL}, \texttt{FEAR\_URGENCY}, \texttt{PRICE\_PROMOTION}, \texttt{IS\_ADS} & Các nhãn nhị phân của bài toán phân loại đa nhãn. Mỗi trường thường nhận giá trị 0/1, trong đó 1 thể hiện đoạn văn bản có dấu hiệu tương ứng. \\ \hline
\texttt{EFFICACY\_CLAIM\_reason}, \texttt{CURE\_CLAIM\_reason}, \texttt{AUTHORITY\_REFERENCE\_reason}, \texttt{TESTIMONIAL\_reason}, \texttt{FEAR\_URGENCY\_reason}, \texttt{PRICE\_PROMOTION\_reason}, \texttt{IS\_ADS\_reason} & Lý do hoặc bằng chứng gán nhãn cho từng nhóm dấu hiệu. Các trường này hỗ trợ kiểm tra chất lượng nhãn, giải thích kết quả và rà soát lại các trường hợp gây nhầm lẫn. \\ \hline
\texttt{label\_count} & Số lượng nhãn dương trên một mẫu, dùng để thống kê mức độ chồng lấn nhãn và hỗ trợ kiểm tra phân bố dữ liệu. \\ \hline
\end{tabularx}
\end{table}

\subsection{Dữ liệu hình ảnh}
Với dữ liệu hình ảnh, đề tài sử dụng Roboflow để quản lý ảnh, gán nhãn bounding box, chia tập và export dữ liệu phục vụ huấn luyện YOLO. Vì Roboflow có thể export sang nhiều định dạng khác nhau, báo cáo chỉ cố định định dạng ở mức thực nghiệm: dataset được export theo YOLO/Ultralytics format. Khi đó, dữ liệu không nhất thiết nằm trong một bảng CSV như nhánh văn bản, mà thường được tổ chức theo thư mục ảnh, thư mục nhãn và file cấu hình dataset.

\begin{table}[H]
\centering
\caption{Cấu trúc dữ liệu hình ảnh khi export từ Roboflow theo YOLO format}
\label{tab:image_data_format}
\begin{tabularx}{\textwidth}{|L{0.32\textwidth}|Y|}
\hline
\textbf{Thành phần} & \textbf{Ý nghĩa} \\
\hline
\texttt{train/}, \texttt{valid/}, \texttt{test/} & Các thư mục dữ liệu sau khi Roboflow chia tập. Tùy cấu hình export, tên validation có thể là \texttt{valid} hoặc \texttt{val}. \\ \hline
\texttt{images/} & Thư mục chứa các file ảnh dùng cho huấn luyện và đánh giá. Mỗi ảnh tương ứng với một file annotation trong thư mục \texttt{labels/} nếu ảnh có đối tượng được gán nhãn. \\ \hline
\texttt{labels/} & Thư mục chứa các file nhãn dạng \texttt{.txt}. Mỗi file nhãn có cùng tên gốc với ảnh tương ứng. \\ \hline
\texttt{data.yaml} & File cấu hình dataset, khai báo đường dẫn train/validation/test, số lớp và tên lớp như \texttt{Lab-coat}, \texttt{logo}, \texttt{stethoscope}, \texttt{Shirt}. \\ \hline
\texttt{*.txt} annotation & Mỗi dòng trong file nhãn thường có dạng \texttt{class\_id x\_center y\_center width height}. Các tọa độ được chuẩn hóa theo kích thước ảnh, nằm trong khoảng từ 0 đến 1. \\ \hline
\texttt{metadata} từ Roboflow & Roboflow có thể lưu thêm thông tin version, preprocessing, augmentation hoặc thông tin nguồn. Các thông tin này cần được ghi lại trong báo cáo thực nghiệm để tái lập quá trình huấn luyện. \\ \hline
\end{tabularx}
\end{table}

Như vậy, phần văn bản được mô tả theo schema bảng cụ thể, còn phần hình ảnh được mô tả theo cấu trúc export của Roboflow. Khi cần liên kết hai nguồn dữ liệu ở tầng hợp nhất, có thể bổ sung bảng metadata riêng để nối \texttt{post\_id} hoặc URL bài đăng với các file ảnh/frame tương ứng.

\section{Bộ nhãn sử dụng}
\subsection{Bộ nhãn văn bản}
\begin{table}[H]
\centering
\caption{Bộ nhãn phân loại văn bản}
\label{tab:text_label_set}
\begin{tabularx}{\textwidth}{|L{0.34\textwidth}|Y|}
\hline
\textbf{Nhãn} & \textbf{Ý nghĩa} \\
\hline
\texttt{IS\_ADS} & Nội dung có bản chất quảng cáo hoặc bán hàng. \\ \hline
\texttt{EFFICACY\_CLAIM} & Có tuyên bố công dụng, hiệu quả hoặc lợi ích sức khỏe. \\ \hline
\texttt{CURE\_CLAIM} & Có tuyên bố chữa bệnh, điều trị, thay thế thuốc hoặc khỏi bệnh. \\ \hline
\texttt{AUTHORITY\_REFERENCE} & Có sử dụng hoặc gợi nhắc uy tín bác sĩ, bệnh viện, cơ quan, chuyên gia hoặc truyền thông. \\ \hline
\texttt{TESTIMONIAL} & Có câu chuyện nhân chứng, phản hồi khách hàng hoặc trải nghiệm khỏi bệnh. \\ \hline
\texttt{FEAR\_URGENCY} & Có yếu tố hù dọa, cảnh báo biến chứng, khan hiếm hoặc thúc ép hành động. \\ \hline
\texttt{PRICE\_PROMOTION} & Có ưu đãi, giảm giá, khuyến mãi, cam kết hoàn tiền hoặc deal giới hạn. \\ \hline
\end{tabularx}
\end{table}

\subsection{Bộ nhãn hình ảnh}
\begin{table}[H]
\centering
\caption{Bộ nhãn hình ảnh dùng cho object detection}
\label{tab:image_label_set}
\begin{tabularx}{\textwidth}{|L{0.22\textwidth}|L{0.20\textwidth}|Y|}
\hline
\textbf{Nhãn} & \textbf{Vai trò} & \textbf{Ý nghĩa} \\
\hline
\texttt{Lab-coat} & Lớp mục tiêu chính & Phát hiện áo blouse trắng, thường liên quan đến hình ảnh bác sĩ, chuyên gia y tế hoặc nhân vật được trình bày như chuyên gia. \\ \hline
\texttt{logo} & Lớp mục tiêu chính & Phát hiện logo hoặc biểu tượng xuất hiện trong ảnh quảng cáo, đặc biệt các logo có thể liên quan đến cơ quan, truyền thông hoặc tổ chức uy tín. \\ \hline
\texttt{stethoscope} & Lớp mục tiêu chính & Phát hiện ống nghe y tế, hỗ trợ nhận diện bối cảnh y khoa hoặc hình ảnh chuyên môn y tế. \\ \hline
\texttt{Shirt} & Lớp phụ/đối chứng & Phát hiện áo thông thường nhằm giúp mô hình phân biệt với áo blouse y tế. Lớp này không phải dấu hiệu rủi ro pháp lý độc lập. \\ \hline
\end{tabularx}
\end{table}

\section{Nguyên tắc gán nhãn}
\subsection{Dữ liệu văn bản}
Mỗi mẫu văn bản được gán nhãn dựa trên nội dung thực tế và ngữ cảnh sử dụng, không chỉ dựa vào sự xuất hiện của từ khóa. Với nội dung dài, dữ liệu có thể được chia thành các đoạn phù hợp với đầu vào của mô hình, nhưng cần giữ liên kết với \texttt{post\_id} gốc để kiểm soát leakage.

Các nhãn rủi ro được hiểu là tín hiệu hỗ trợ rà soát. Một mẫu có thể đồng thời có nhiều nhãn, ví dụ vừa là quảng cáo, vừa tuyên bố công dụng, vừa lợi dụng uy tín chuyên gia. Evidence nên được lưu lại khi có đoạn văn bản làm căn cứ rõ ràng.

\subsection{Dữ liệu hình ảnh}
Mỗi ảnh hoặc frame được gán nhãn theo đối tượng thị giác xuất hiện trong ảnh. Annotation cần xác định rõ bounding box của đối tượng, đặc biệt với các lớp dễ nhầm lẫn như \texttt{Lab-coat} và \texttt{Shirt}. Việc thêm \texttt{Shirt} nhằm tạo lớp đối chứng để giảm nhầm lẫn trang phục, không nhằm mở rộng nhóm vi phạm chính.

Các nhãn \texttt{Lab-coat}, \texttt{logo} và \texttt{stethoscope} được xem là dấu hiệu hình ảnh có thể làm tăng rủi ro khi kết hợp với nội dung quảng cáo. Tuy nhiên, chỉ riêng sự xuất hiện của các đối tượng này chưa đủ để kết luận vi phạm; kết quả cần được hợp nhất với OCR, NLP, ASR, rule-based detection và ngữ cảnh bài đăng.

\section{Chia tập dữ liệu}
\subsection{Dữ liệu văn bản}
Dữ liệu văn bản được chia thành train, validation và test theo \texttt{post\_id} để các đoạn thuộc cùng một bài đăng không xuất hiện đồng thời ở nhiều tập. Cách chia này giúp đánh giá khách quan hơn khả năng tổng quát hóa của mô hình trên bài đăng mới.

\subsection{Dữ liệu hình ảnh}
Dữ liệu hình ảnh cần được chia theo nguồn media gốc, video gốc hoặc \texttt{post\_id} nếu có, nhằm tránh trường hợp các frame gần giống nhau xuất hiện ở cả train và test. Với dữ liệu Roboflow, cần lưu rõ version dataset, format export và các cảnh báo liên quan đến annotation để thuận tiện tái lập thực nghiệm.
